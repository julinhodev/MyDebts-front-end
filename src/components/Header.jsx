import './Header.scss';

const Header = () => {
    return(
        <div className="header-container">
            <nav className="nav-container">
                 <span className="header-logo">My Debts</span>
            </nav>
        </div>
    );
};

export default Header;